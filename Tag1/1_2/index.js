import { names, numbers } from "./data.js";
import { allButFirst, allButLast, cap, capAll, check, double, firstOF, last, numBetween, sum } from "./functions.js";

firstOF(names, numbers)
allButLast(names, numbers)
last(names, numbers)
allButFirst(names, numbers)
double(numbers)
sum(numbers)
numBetween(2, 5)
cap('peter')
capAll('peter')
check('peter', 'ralf')
